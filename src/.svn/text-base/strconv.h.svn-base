/* $Id: strconv.h 211 2008-08-22 14:12:06Z roe $ */

#ifndef STRCONV_H
#define STRCONV_H

size_t ucs2_from_wcs(uint8_t *, size_t, wchar_t *);
void ucs2_to_wcs(uint16_t *, size_t, wchar_t *);

/* Checks of two directory entry names are the same. */
int is_same_name(const uint16_t *name1, size_t size1, 
                 const uint16_t *name2, size_t size2);

#endif /* STRCONV_H */

